1. Determine how many times Plugin and Dictator get instantiated using Class and Instance counter variables
    1. No logs created when plugin first pushed to Deck
    2. One log file created when Decky menu opened to settings
        1. Plugin and Dictator instantiated 6 times and then deleted 5 times. Process ends between each instantiation so instance count remains at 1. Final instances seem to remain open.
    3. Final instantiation (number 6) is used subsequently.
    4. Disabling plugin does not terminate the instances

2. Review code to determine feasibility of modifying hot keys
3. Document all changes to codebase
4. Create pull request to original library
5. Modify the plugin to support saving settings
6. Modify nerd-dication to support common game command (slash, backslash, etc.)
7. Modify the plugin to support customizable custom game commands 


Notes:
Decky API pre-V1 did not instantiate Plugin, it's just a namespace basically so "self" doesn't work.
dotool works fine but seems to require this to be set in a Python script: os.environ["XDG_RUNTIME_DIR"] = "/run/user/1000"
Logs are saved to /tmp on the Steam Deck


Questions:
Should the Dictator class store/check for PID and running before trying to create a new process?
Should the Dictator class return True if dictation is started?
Should the Plugin class check on the status of a begin/end request?
Should the Plugin class report status back to the front end?
What kind of logging and error handling should be configured?
Is all of the async/await stuff right?
Why does dotool work without being in the input group on the Steam Deck?
Should I try using ydotool with a daemon running in user mode mode as a process?




Linux permissions:
`groups` to see which groups
getfacl /dev/uinput to see what ACL rules apply to /dev/uinput
grep uinput /etc/udev/rules.d/* /lib/udev/rules.d/* to see 


Python class:
might use @classmethod instead of creating a real singleton for Dictator class